/*//Receba do usuário uma data no formato YYYY-MM-DD:

var data = prompt("Digite uma data no formato YYYY-MM-DD: ")  
//Retorne com o window.alert a data no formato DD/MM/YYYY:

var data2 = data.split("-")
 
window.alert (data2[2] + "/" + data2[1] + "/" + data2[0]);*/
/*___________________________________________________________________________________________*/

// Inicializa o carrinho de compras como uma lista vazia
/*let carrinho = [];

// Loop para solicitar frutas ao usuário
while (true) {
  // Solicita ao usuário a inclusão de uma fruta
  let fruta = prompt("Digite o nome de uma fruta para adicionar ao carrinho (ou 'Sair' para parar):");

  // Verifica se o usuário digitou 'Sair'
  if (fruta === "Sair") {
    // Sai do loop e exibe a listagem de frutas
    break;
  }

  // Adiciona a fruta na primeira posição da lista
  carrinho.unshift(fruta);

  // Verifica se o número de itens no carrinho ultrapassou 5
  if (carrinho.length > 5) {
    // Remove a fruta mais antiga da lista
    carrinho.pop();
  }
}

// Exibe a listagem de frutas no console
console.log("Listagem de frutas:");
console.log(carrinho);*/

/*_________________________________________________________________*/

/*
// Inicializa o carrinho de compras como uma lista vazia
let carrinho = [];

// Loop para solicitar frutas ao usuário
while (true) {
  // Solicita ao usuário a inclusão de uma fruta
  let fruta = prompt("Digite o nome de uma fruta para adicionar ao carrinho (ou 'Sair' para parar):");

  // Verifica se o usuário digitou 'Sair'
  if (fruta === "Sair") {
    // Sai do loop e exibe a listagem de frutas
    break;
  }

  // Adiciona a fruta no final da lista
  carrinho.push(fruta);

  // Verifica se o número de itens no carrinho ultrapassou 5
  if (carrinho.length > 5) {
    // Remove a fruta mais antiga da lista (a primeira que foi adicionada)
    carrinho.shift();
  }
}


// Exibe a listagem de frutas no console
console.log("Listagem de frutas:");
console.log(carrinho);*/
/*____________________________________________________________________*/

/*
//Exemplo de for of

//array de exemplo 
var array = [123, 'texto', true, 'palavra', 99, 'JS'];

//usando for of
for (var item of array){

    //verificando se o item é uma string
    if (typeof item === 'string'){
        //exibindo
        console.log(item);
    }
}
*/
/*_________________________________________________________________________*/




        



